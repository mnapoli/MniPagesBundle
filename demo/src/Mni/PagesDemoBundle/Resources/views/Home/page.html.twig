{% extends '::layout.html.twig' %}

{% block content %}

    <div class="jumbotron">

        <h1>{{ title }}</h1>
        <p class="lead">This is a demo of the MniPagesBundle.</p>

        <form class="form-inline" data-page-action="setTitle" data-page-refresh>
            <div class="form-group">
                <input type="text" class="form-control" name="title" placeholder="{{ title }}">
            </div>
            <button type="submit" class="btn btn-default">Update title</button>
        </form>

    </div>

    {{ component('MniPagesDemoBundle:RandomNumber') }}

    {{ component('MniPagesDemoBundle:RandomNumber') }}

{% endblock %}
